#include "../include/MapItem.h"
#include "../include/FrequencyNode.h"

// Define member function implementations for Item class
// Include necessary headers for other classes if required

MapItem::MapItem(int value, FrequencyNode *parent, NodeList *node) : value(value), parent(parent), node(node) {
    // Initialize the Item object with the provided values
}
